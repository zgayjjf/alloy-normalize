pipeline:
  build:
    image: node:8
    commands:
      - npm install
      - npm run test
      - npm run build
  deploy:
    image: drillster/drone-rsync
    hosts: ['119.28.114.230']
    user: ubuntu
    secrets: [ rsync_key ]
    source: ./
    target: ~/normalize
    script:
      - echo "rsync success1"
    when:
      branch: release